import 'package:flutter/material.dart';

class AllStudentsListRegistration extends StatelessWidget {
  const AllStudentsListRegistration({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}